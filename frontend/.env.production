# Production build config for Vite.
#
# Frontend and backend share the same origin (workflow.deepmandloi.com),
# so the frontend uses relative URLs and no API base URL needs to be set.
# Add VITE_* variables here if you ever need build-time config for prod.
