# Production build config. Baked into the Vite build at image-build time.
# Must be an ABSOLUTE URL because the runtime code uses `|| 'http://localhost:8001'`
# as a fallback — an empty string would fall through to localhost.
VITE_API_URL=https://workflow.deepmandloi.com
