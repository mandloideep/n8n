ENV=development
DATABASE_URL=sqlite:///./test.db
API_PREFIX=/api

# Local-only dev key. Replace with `openssl rand -hex 32` if you want a fresh one.
# DO NOT use this value in production — set SECRET_KEY in Dokploy instead.
SECRET_KEY=dev_only_secret_replace_me_locally_with_openssl_rand_hex_32_xx

# Local-only Fernet key. Generate a fresh one with:
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# DO NOT use this value in production — set CREDENTIALS_ENCRYPTION_KEY in Dokploy.
CREDENTIALS_ENCRYPTION_KEY=dXuS0HtnoG6yMRoy24fxhB6CkhUYAd2vBgVcyflDmF8=

ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:8001,http://localhost:8080

OPENAI_API_KEY=

TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
